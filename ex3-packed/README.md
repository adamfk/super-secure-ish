Run the python3 script `pack-for-wokwi.py` to update the "packed" file in this directory.

It basically just appends all of the source files together in a valid order so that you can easily copy paste into wokwi for hardware simulation.